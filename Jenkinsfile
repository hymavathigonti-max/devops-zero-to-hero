pipeline {
    agent any

    stages {

        stage('Clone Verification') {
            steps {
                echo 'Hello Hymavathi!'
            }
        }

        stage('Workspace Info') {
            steps {
                sh 'pwd'
                sh 'ls -R'
            }
        }
    }
}
